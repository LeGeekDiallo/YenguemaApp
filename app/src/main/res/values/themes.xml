<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Yenguema" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryDarkColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primaryColor</item>
        <item name="colorSecondaryVariant">@color/primaryLightColor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="HomeTitle">
        <item name="android:textSize">25sp</item>
        <item name="android:text">@string/home_title</item>
        <item name="android:fontFamily">@font/oswald_regular</item>
        <item name="android:textColor">@color/primaryDarkColor</item>
        <item name="android:textAlignment">center</item>
    </style>
    <style name="ServicesItem">
        <item name="android:textSize">15sp</item>
        <item name="android:text">@string/home_title</item>
        <item name="android:fontFamily">@font/oswald_regular</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAlignment">center</item>
    </style>
    <style name="LogInText">
        <item name="android:textSize">13sp</item>
        <item name="android:text">@string/password</item>
        <item name="android:fontFamily">@font/oswald_regular</item>
        <item name="android:textColor">@color/primaryDarkColor</item>
    </style>
</resources>